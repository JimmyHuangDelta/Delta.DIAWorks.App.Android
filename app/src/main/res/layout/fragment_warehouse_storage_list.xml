<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.delta.android.WMS.Client.Fragment.WarehouseStorageListFragment">

    <RelativeLayout

        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ListView
            android:id="@+id/lvWvLotData"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_above="@+id/btnConfirm"/>

        <Button
            android:id="@+id/btnConfirmAll"
            style="@style/CommonRoundedButton"
            android:layout_alignParentBottom="true"
            android:text="@string/WAREHOUSE_STORAGE_ALL" />
    </RelativeLayout>

</FrameLayout>
